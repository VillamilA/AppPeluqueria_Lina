import 'package:flutter/material.dart';

class AppColors {
  static const charcoal = Color(0xFF181818); // Negro elegante
  static const gold = Color(0xFFFFC93C);     // Dorado
  static const gray = Color(0xFFBDBDBD);     // Gris para textos secundarios
}
